/dts-v1/;

/ {
    description = "IOT2050 secure boot OTP command data - Only Enabling";
    creator = "IOT2050 secure boot OTP provisioning tool";

    images {
    };

    options {
        version = <1>;
        option-1 {
            cmd = "enable-secure-boot";
        };
    };
};
